import 'package:flutter/material.dart';

class Panel2 extends StatelessWidget {
  const Panel2({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Contenido Panel 2'));
  }
}
