import 'package:flutter/material.dart';

class Panel1 extends StatelessWidget {
  const Panel1({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Contenido Panel 1'));
  }
}
